"""Investigation tools: small, typed, allowlisted capabilities the agent may
call (list/search the manifest, look inside workbooks and documents,
Decimal arithmetic, bounded table comparison, an isolated Python sandbox).
Tools return data with provenance; they never write domain records."""
